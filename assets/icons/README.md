Source SVGs for the three navigation glyphs (menu, sun, moon), from
[Phosphor Icons](https://phosphoricons.com/), regular weight, MIT licensed.

The paths are inlined into each page so they inherit `currentColor`. These
files are kept as the provenance of those paths; nothing loads them at runtime.
